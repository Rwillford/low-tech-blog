{{>post-info post}}

{{#if loggedIn}}
<form class="comment-form">
    <div>
        <testarea name="comment-body"></testarea>
    </div>
                        
    <div>
        <button type="submit">Add Comment</button>
    </div>
</form>
{{/if}}

{{>comments post.comments}}

{{#if loggedIn}}
{{!Javascript goes here}}
{{/if}}
